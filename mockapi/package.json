{
  "name": "mockapi",
  "version": "1.0.0",
  "description": "JSON server to mock out a backend during development",
  "main": "index.js",
  "scripts": {
    "docker-build": "docker build -t json-server .",
    "docker-run": "docker run -d -p 3000:3000 -v $(pwd):/app json-server && sleep 3 && open http://localhost:3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
